'birds
'it just multiplies two numbers idk
Module Module1
    Sub Main()
        Dim x As Integer = 2
        Dim y As Integer = 3
        Dim z As Integer = x * y
        Console.Write("Your number is " & z)
        Console.Read()
    End Sub
End Module