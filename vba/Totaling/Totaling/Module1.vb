Module Module1 'gets your input and adds it to the sum of your previous inputs. does that the amount of times your number was. im bad at words.

    Sub Main()
        Dim n As Integer = 0
        Dim total As Integer
        Dim counter As Integer = 0
        n = Console.ReadLine()
        dim x as integer = 1
        For x = 1 To n
            total = total + x
        Next
        Console.WriteLine(total)
        Console.ReadKey()



    End Sub

End Module